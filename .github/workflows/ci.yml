name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  ci:
    permissions:
      contents: read
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version: "lts/*"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install workflow linters
        if: runner.os == 'Linux'
        shell: bash
        run: |
          go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.12
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
          pipx install zizmor==1.27.0

      - name: Build
        run: npm run build

      - name: Test
        # exec.test.ts is skipped on Windows (POSIX process groups), so
        # branch coverage falls below the threshold. Coverage thresholds
        # are enforced on Linux and macOS where all suites run.
        run: ${{ runner.os == 'Windows' && 'npx vitest run' || 'npm test' }}

      - name: Check
        if: runner.os == 'Linux'
        run: npm run check
